# Photo Robot - backend dependencies
# Python 3.12 on Windows.  Everything here has a prebuilt wheel; no compiler
# is required, which matters because this is installed on the client machine.

# --- web ---------------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4

# --- imaging / computer vision ----------------------------------------
numpy==1.26.4              # pinned <2 for mediapipe/opencv wheel compatibility
pillow==11.1.0
opencv-contrib-python==4.10.0.84
scipy==1.14.1
scikit-image==0.25.0
mediapipe==0.10.20         # pose, face mesh, hands, selfie segmentation

# --- providers ---------------------------------------------------------
httpx==0.28.1
requests==2.32.3
